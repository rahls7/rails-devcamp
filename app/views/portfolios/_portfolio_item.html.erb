<p><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item.id) %></p1>
<p><%= portfolio_item.subtitle%></p1>
<p><%= portfolio_item.body%></p1>
<%= image_tag portfolio_item.thumbnail unless portfolio_item.thumbnail.nil? %>
<%= link_to "Edit portfolio_item", edit_portfolio_path(portfolio_item.id) if logged_in?(:site_admin) %>
<%= link_to 'Delete Portfolio portfolio_item', portfolio_path(portfolio_item.id), method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin) %>
